<Window x:Class="Taskalu.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Taskalu"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance {x:Type local:MainViewModel}}" 
        Title="MainWindow" Height="768" Width="1024">

    <Grid>
        <Grid.Resources>
            <Style TargetType="TextBlock" x:Key="label1">
                <Setter Property="Margin" Value="4,4,2,-2"/>
                <Setter Property="Foreground" Value="#FF5A6779" />
                <Setter Property="Width" Value="60" />
                <Setter Property="TextAlignment" Value="Right" />
            </Style>
            <Style TargetType="TextBlock" x:Key="label2">
                <Setter Property="Margin" Value="4,4,2,-2"/>
                <Setter Property="Foreground" Value="#FF1E395B"/>
            </Style>
            <Style TargetType="ListBoxItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            </Style>
        </Grid.Resources>
        <DockPanel>
        <StackPanel DockPanel.Dock="Top" VerticalAlignment="Top">
            <Menu VerticalAlignment="Top">
                <MenuItem Header="_File">
                    <MenuItem Header="e_xit" Click="Exit_Click" />
                </MenuItem>
                <MenuItem Header="_view">
                    <MenuItem Header="" />
                </MenuItem>
            </Menu>
            <ToolBarTray VerticalAlignment="Top">
                <ToolBar Height="26" VerticalAlignment="Top">
                    <Button Name="NewButton" Content="New Task" HorizontalAlignment="Left" Margin="0,0,0,0" VerticalAlignment="Top" Width="75" Click="NewButton_Click"/>
                </ToolBar>
            </ToolBarTray>
        </StackPanel>
        <StatusBar DockPanel.Dock="Bottom" Height="20" VerticalAlignment="Bottom"/>
        <ListBox x:Name="listbox1" DockPanel.Dock="Top" BorderThickness="0" Margin="0,0,0,0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ItemsSource="{Binding Files}" SelectionChanged="ListSelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="0,0,0,1" BorderBrush="#FFDCDCDC" Margin="17,0,10,0">
                        <Grid Height="72" HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition />
                                <ColumnDefinition Width="180" />
                            </Grid.ColumnDefinitions>
                            <Border Background="White" Margin="0,7,0,18" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <Border.Effect>
                                    <DropShadowEffect Color="Gray" Direction="-45" Opacity="0.5" ShadowDepth="2" />
                                </Border.Effect>
                                <Image Source="{Binding Name}" Width="29" />
                            </Border>
                            
                            <StackPanel Grid.Column="1" Margin="0,5" HorizontalAlignment="Left" >
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="14" Margin="4,4" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Type}" Style="{StaticResource label2}" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Grid.Column="2" Margin="0,5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="期限:" Style="{StaticResource label1}" />
                                    <TextBlock Text="{Binding CreateDate}" Style="{StaticResource label2}" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="優先度:" Style="{StaticResource label1}" />
                                    <TextBlock Text="{Binding Size}" Style="{StaticResource label2}" />
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
            <StackPanel DockPanel.Dock="Top" Name="stackpanel1" Visibility="Collapsed" >
                <TextBox Name="textbox1" Height="23" TextWrapping="Wrap" Text="" Width="120"/>
                <Button Name="button1" Content="Close" Width="75" Click="Button1_Click"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
